{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CCPL with Test File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/ccpl/build/ccpl",
            "args": [
                "${input:testFile}"
            ],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/ccpl",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "meson compile",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ],
    "inputs": [
        {
            "id": "testFile",
            "type": "promptString",
            "description": "Enter test file path",
            "default": "${file}"
        }
    ]
}
